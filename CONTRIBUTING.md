# Contributing to LitRun!

感谢你帮助改进不熄！/ LitRun!。这是一个面向 macOS 的原生 Swift/AppKit 工具，欢迎 Bug 报告、兼容性反馈、文档改进和小而清晰的 Pull Request。

## 开始之前

- 先搜索已有的 Issue，避免重复提交。
- 新功能先描述用户问题、预期行为和安全边界。
- 不要提交密码、Token、邮箱验证码、私钥、完整系统日志或其他私人数据。
- 不要用真实的高温、密闭包或 0% 风扇设置做压力测试。

## 本地构建和测试

在 macOS 13+ 上执行：

```bash
./scripts/build.sh
./scripts/test.sh
./scripts/test_x86_64.sh
```

涉及发布包时再运行：

```bash
./scripts/package_release.sh
```

请在 Pull Request 中说明实际运行了哪些命令。编译通过、Rosetta 通过和真实硬件验证是不同层级，不要把其中一个写成全部硬件都已验证。

## 代码约定

- 保持 AppKit 原生控件、可访问性标签和现有本地化结构。
- 把 UI、模式策略、遥测、恢复和系统 I/O 留在各自模块中。
- 系统设置必须可恢复；不要写入没有明确回滚路径的全局状态。
- 功耗、温度、风扇和兼容性数据不可用时，显示不可用，不要猜测。
- 新行为优先添加针对性的 Smoke Test，并保持警告视为错误。
- 不要把 `build/`、`dist/`、本机 App、截图缓存或证书加入源码提交。

## Pull Request

请使用清晰的标题，并在描述中包含：

1. 解决了什么问题。
2. 改变了哪些用户行为。
3. 测试命令和结果。
4. 是否需要真实 Intel、Apple Silicon、风扇或合盖硬件验证。
5. 是否有恢复、权限、隐私或兼容性影响。

小而集中的 PR 更容易审核。涉及电源、风扇、合盖、恢复和权限的改动，需要先说明失败时如何恢复。

## Issue 报告

请尽量提供 App 版本、macOS 版本、芯片类型、Mac 型号、复现步骤和不含私人数据的错误信息。高温或异常退出问题请同时说明当时的电源、合盖、低功耗和风扇状态。

## 许可

提交代码即表示你同意该贡献按本项目的 [MIT License](LICENSE) 发布。
